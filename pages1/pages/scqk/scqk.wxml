<!--pages/pages1/scqk/scqk.wxml-->
<view class="TT">
  <!-- <van-empty description="暂无预约"/> -->
  <view class="yynr">
    <view class="cell" wx:for="{{shenhe}}" wx:key="shenhe" wx:for-index="index"> 
        <van-cell  title="{{item.bname}}" data-index='{{index}}' value="{{item.zt}}" size="mini" label="{{item.time}}" />
    </view>
    <block wx:if="{{dataILu}}">
    <view class="loadMore">正在加载中...</view>
  </block>
  </view>
</view>