<!--pages2/blsh/blsh.wxml-->
<view class="TT">
  <view class="T">
    <view class="cell" wx:for="{{sh}}" wx:key="sh" wx:for-index="index" data-index='{{index}}' bindtap="shqk">
      <image src="{{item.avatarUrl}}"></image>
      <view class="name">
        <view class="nm">
          <text>{{item.name}}</text>
        </view>
        <view class="bl">
          <text>{{item.bname}}</text>
        </view>
      </view>
    </view>
    <block wx:if="{{dataILu}}">
    <view class="loadMore">正在加载中...</view>
  </block>
  </view>
</view>

